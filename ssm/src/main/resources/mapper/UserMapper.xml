<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mixin.demo.ssm.mybatis.dao.UserDao" >
    <sql id="BASE_TABLE">
        t_user
    </sql>

    <sql id="BASE_COLUMN">
        user_id,user_name,password,phone
    </sql>

    <insert id="insert" parameterType="com.mixin.demo.ssm.mybatis.entity.UserDomain">
        INSERT INTO
        <include refid="BASE_TABLE"/>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            user_name,password,
            <if test="phone != null">
                phone,
            </if>
        </trim>
        <trim prefix="VALUES(" suffix=")" suffixOverrides=",">
            #{user_name, jdbcType=VARCHAR},#{password, jdbcType=VARCHAR},
            <if test="phone != null">
                #{phone, jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>

    <delete id="deleteUserById">
        DELETE FROM
        <include refid="BASE_TABLE"/>
        WHERE
        user_id = #{user_id, jdbcType=INTEGER}
    </delete>
    <!-- 更新用户信息，为空的字段不进行置空 -->
    <update id="updateUser" parameterType="com.mixin.demo.ssm.mybatis.entity.UserDomain">
        UPDATE
        <include refid="BASE_TABLE"/>
        <set>
            <if test="user_name != null">
                user_name = #{user_name, jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                password = #{password, jdbcType=VARCHAR},
            </if>
            <if test="phone != null">
                phone = #{phone, jdbcType=VARCHAR},
            </if>
        </set>
        <where>
            user_id = #{user_id, jdbcType=INTEGER}
        </where>
    </update>

    <select id="selectUsers" resultType="com.mixin.demo.ssm.mybatis.entity.UserDomain">
        SELECT
        <include refid="BASE_COLUMN"/>
        FROM
        <include refid="BASE_TABLE"/>
    </select>

    <select id="selectById" resultType="com.mixin.demo.ssm.mybatis.entity.UserDomain">
        SELECT
        <include refid="BASE_COLUMN"/>
        FROM
        <include refid="BASE_TABLE"/>
        <where>
            user_id = #{user_id, jdbcType=INTEGER}
        </where>
    </select>
</mapper>